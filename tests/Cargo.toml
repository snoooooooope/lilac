[package]
name = "lilac-tests"
version = "0.1.0"
edition = "2024"

[dependencies]
lilac = { path = ".." }
tempfile = "3.20.0"

[dev-dependencies]
mockito = "1.4.0"
tokio = { version = "1.45.0", features = ["rt-multi-thread"] } 